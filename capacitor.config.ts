import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple_ui_elements',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
